<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="{% if pagination.items[0].data.title %}{{ pagination.items[0].data.title | safe }}{% else %}Experience the cutting-edge wonders of our mobile app development, web development, data analysis, and unrivaled cyber security. We redefine the very essence of IT outsourcing.{% endif %}">
     <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            rel="stylesheet"
            href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0"
        />
        <link
            href="https://fonts.googleapis.com/css2?family=Raleway:wght@300;500;600;700;800;900&display=swap"
            rel="stylesheet"
        />
        <link rel="stylesheet" href="{{ '/assets/css/swiper-bundle.min.css' }}" />
     <link rel="stylesheet" href="{{ '/assets/css/tailwind.css' | url }}">

        <!-- box icon -->
        <link
            href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
            rel="stylesheet"
        />
        <!-- ==========================AOS========================= -->
        <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
        <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
        <!-- ==========================AOS========================= -->
        <!-- swiper js -->
        <script src="{{ '/assets/js/swiper-bundle.min.js' }}" type="module" defer></script>

        <script type="module" src="{{ '/assets/js/app.js' }}" defer></script>
            <title>{{ title }}</title>
</head>
<body>
    {% block title %}
    {% endblock %}

    {{ content | safe }}
    {% include "footer.njk" %}
    <script>
            AOS.init({
                offset: 200,
                delay: 10,
                duration: 800,
                easing: "ease-in-out",
                once: true,
            });
    </script>
    <script src="{{ '/assets/js/form.js/' }}"></script>
</body>
</html>

    {# "scripts": {
        "start": "npm-run-all -p dev:*",
        "build": "run-s build:*",
        "dev:11ty": "eleventy --serve",
        "dev:css": "tailwindcss -i src/assets/css/tailwind.css -o _site/assets/css/tailwind.css --watch --postcss",
        "build:11ty": "eleventy",
        "build:css": "tailwindcss -i src/assets/css/tailwind.css -o _site/assets/css/tailwind.css --postcss"
    }, #}
    {# types: [contentful-update] #}