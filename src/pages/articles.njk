---
title: Articles | Find latest articles that are sure to keep you captivated here
layout: "baseOthers.njk"
permalink: /articles/
---

<header
            class="w-full lg:h-[75vh] md:max-h-[500px] pt-14 pb-10 overflow-hidden bg-pattern2 mt-20 lg:mt-0"
            id="header"
        >
            <div
                class="w-full h-full relative flex flex-col lg:flex-row lg:items-center lg:justify-between max-w-6xl mx-auto px-5 lg:px-0 lg:gap-20"
            >
                <div class="min-h-28">
                    <div class="hero2-content lg:max-w-[100%]">
                        <h2 class="header2-h1">
                            Read Latest articles
                        </h2>
                        <p class="header2-p">
                            The very latest from us with deep insights, thoughts and resources on building your Brand
                        </p>
                        <!-- links -->
                        <div
                            class="flex justify-between md:justify-start md:gap-7 items-center text-pbase lg:text-base w-full mt-4"
                        >
                            <a
                                href="/contact/"
                                class="py-3 lg:py-4 px-6 lg:px-8 bg-phosBlue2 hover:bg-phosBlue transition-all duration-300 border border-gray-50 rounded-[35px] text-gray-50 text-lg font-semibold"
                                >Contact Us</a
                            >
                            <a
                                href="/service/"
                                class="py-3 lg:py-4 px-6 lg:px-8 bg-gray-200 hover:bg-gray-300 transition-all rounded-[35px] text-grey-950 text-lg font-semibold shadow-md"
                                >Services</a
                            >
                        </div>
                        <!-- links end -->
                    </div>
                </div>
            </div>
</header>
<!-- header section ends -->
<main class="py-10 md:pb-20 lg:pt-0 px-5 md:px-10 w-full bg-gray-50 max-w-[1400px] mx-auto relative">
    <div class="">
        {% set article1 = collections.contentfulArticles | first %} <!-- | reverse -->
        <a href="/articles/{{ article1.fields.slug }}">
            <div class="flex flex-col lg:grid lg:grid-cols-2 lg:gap-7 p-4 rounded-sm shadow-md max-w-2xl lg:max-w-none mx-auto bg-white">
                <img src="{{ article1.fields.featuredImage.fields.file.url }}" alt="{{ article1.fields.featuredImage.fields.description }}"
                    class="aspect-video object-cover overflow-hidden rounded" />
                <div class="px-0 py-4 lg:px-4">
                    <div class="flex gap-5 items-center">
                        <span class="text-phosBlue font-bold">Latest:</span>
                        {% if article1.fields.tags > 1 %}
                            {% for item in article1.fields.tags | slice(0, 2)  %}
                                <span class="bg-phosBlue text-pbase text-white font-normal px-[2px] py-[1px] rounded-sm">{{  item }}</span>
                            {% endfor %}
                        {% else %}
                            <span class="bg-phosBlue text-white font-normal px-3 py-2 rounded-sm">{{  article1.fields.tags | first }}</span>
                        {% endif %}
                    </div>
                    <h1 class="font-bold text-base md:text-md lg:text-2xl tracking-tight capitalize  mt-3">{{ article1.fields.title }}</h1>
                    <div class="flex justify-between items-center mt-2">
                        <h6 class="flex items-center gap-3">
                            <img src="{{ article1.fields.author.fields.avatar.fields.file.url }}" alt="author's pic"class="aspect-square w-10 rounded-full object-cover">
                            {{ article1.fields.author.fields.name }}
                        </h6>
                        <h6 class="">{{ article1.fields.publishedDate | formatDate }}</h6>
                    </div>
                    <p class="mt-4 lg:mt-8 text-base px-0 lg:px-5">
                        {{ article1.fields.subtitle | truncateText(250) }}
                    </p>
                </div>
            </div>
        </a>
    </div>
    <div class="grid lg:grid-cols-4 gap-5 mt-10">
        <div class="lg:col-span-3 bg-white rounded-sm p-3 shadow-md flex flex-col gap-4 lg:gap-0">
            {% if collections.contentfulArticles | length >  2 %}
                {% for item in collections.contentfulArticles | limit(1, 3)  %}
                    <a href="/articles/{{ item.fields.slug }}">
                        <div class="flex flex-col lg:grid lg:grid-cols-2 lg:gap-7 my-4 rounded-sm mx-auto bg-white">
                            <img src="{{ item.fields.featuredImage.fields.file.url }}" alt="{{ item.fields.featuredImage.fields.description }}"
                                class="aspect-video object-cover overflow-hidden rounded" />
                            <div class="px-0 lg:px-2 pt-3 lg:pt-0">
                                <div class="flex gap-5 items-center">
                                    {% if item.fields.tags | length > 1 %}
                                        {% for item in item.fields.tags | limit(0, 2)  %}
                                            <span class="bg-phosBlue text-[13px] text-white font-normal p-2 rounded-sm">{{  item }}</span>
                                        {% endfor %}
                                    {% else %}
                                        <span class="bg-phosBlue text-[13px] text-white font-normal p-2 rounded-sm">{{  item.fields.tags | first }}</span>
                                    {% endif %}
                                    {% for item in item.fields.tags  %}
                                    {% endfor %}
                                </div>
                                <h2 class="font-bold text-pbase lg:text-base tracking-tight capitalize  mt-2">{{ item.fields.title }}</h2>
                                <div class="flex justify-between items-center mt-2">
                                    <h6 class="flex items-center gap-3 text-pbase">
                                        <img src="{{ item.fields.author.fields.avatar.fields.file.url }}" alt="author's pic"class="aspect-square w-8 rounded-full object-cover">
                                        {{ item.fields.author.fields.name }}
                                    </h6>
                                    <h6 class="text-[14px]">{{ item.fields.publishedDate | formatDate }}</h6>
                                </div>
                                 <p class="mt-2 lg:mt-4 text-pbase px-0 lg:px-3">
                                    {{ article1.fields.subtitle | truncateText(150) }}
                                </p>
                            </div>
                        </div>
                    </a>
                    {# {% include "articleCard.njk" with item %} #}
                {% endfor %}
            {% else %}
                {% for item in collections.contentfulArticles %}
                    <a href="/articles/{{ item.fields.slug }}">
                        <div class="flex flex-col lg:grid lg:grid-cols-2 lg:gap-7 my-4 rounded-sm mx-auto bg-white">
                            <img src="{{ item.fields.featuredImage.fields.file.url }}" alt="{{ item.fields.featuredImage.fields.description }}"
                                class="aspect-video object-cover overflow-hidden rounded" />
                            <div class="px-0 lg:px-2 pt-3 lg:pt-0">
                                <div class="flex gap-5 items-center">
                                    {% if item.fields.tags | length > 1 %}
                                        {% for item in item.fields.tags | limit(0, 2)  %}
                                            <span class="bg-phosBlue text-[13px] text-white font-normal p-2 rounded-sm">{{  item }}</span>
                                        {% endfor %}
                                    {% else %}
                                        <span class="bg-phosBlue text-[13px] text-white font-normal p-2 rounded-sm">{{  item.fields.tags | first }}</span>
                                    {% endif %}
                                </div>
                                <h2 class="font-bold text-pbase lg:text-base tracking-tight capitalize  mt-2">{{ item.fields.title }}</h2>
                                <div class="flex justify-between items-center mt-2">
                                    <h6 class="flex items-center gap-3 text-pbase">
                                        <img src="{{ item.fields.author.fields.avatar.fields.file.url }}" alt="author's pic"class="aspect-square w-8 rounded-full object-cover">
                                        {{ item.fields.author.fields.name }}
                                    </h6>
                                    <h6 class="text-[14px]">{{ item.fields.publishedDate | formatDate }}</h6>
                                </div>
                                 <p class="mt-2 lg:mt-4 text-pbase px-0 lg:px-3">
                                    {{ item.fields.subtitle | truncateText(150) }}
                                </p>
                            </div>
                        </div>
                    </a>
                {% endfor %}
            {% endif %}
        </div>
        <div class="col-span-1 bg-white rounded-sm p-3 shadow-md hidden lg:block">
            <h3 class="border-b border-b-gray-200 text-phosBlue pb-3 font-bold text-xl">Categories</h3>
            <ul class="divide-y-[1px] divide-gray-100">
                {% for category in collections.articleCategories %}
                    <li class="h-10"><a href="/articles/{{ category | slug }}/" class="text-gray-500 h-full  flex items-center category" id="{{ category }}">{{ category }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
    {% if collections.contentfulArticles | length >  2 %}
        <div class="mx-auto mt-8 flex items-center justify-center gap-3">
            <a href="/articles/1/" class="px-4 md:px-8 py-2 md:py-3 bg-gray-200 border-[1px] border-gray-500 rounded-sm">View all articles &#187;</a>
        </div>
    {% endif %}
</main>